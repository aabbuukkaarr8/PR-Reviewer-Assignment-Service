# PR Reviewer Assignment Service

Микросервис для автоматического назначения ревьюверов на Pull Request'ы, управления командами и участниками.

##  Описание

Сервис предоставляет HTTP API для:
- Создания и управления командами разработчиков
- Автоматического назначения ревьюверов на PR (до 2 активных ревьюверов из команды автора)
- Переназначения ревьюверов
- Управления активностью пользователей
- Получения списка PR, назначенных конкретному пользователю
- Массовой деактивации пользователей команды с автоматическим переназначением ревьюверов в открытых PR

## Быстрый старт

### Требования

- Go 1.25+ (для локальной разработки)
- Docker и Docker Compose (для запуска через Docker)
- PostgreSQL 16+ (если запускаете БД отдельно)

### Запуск через Docker (рекомендуется)

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd PR-Reviewer-Assignment-Service
   ```

2. **Запустите сервисы:**
   ```bash
   docker-compose up -d
   ```

   Это автоматически:
   - Соберет образы для БД и API
   - Запустит PostgreSQL с применением миграций
   - Запустит API сервис на порту 8080

3. **Проверьте статус:**
   ```bash
   docker-compose ps
   ```

4. **Проверьте логи:**
   ```bash
   docker-compose logs -f api
   ```

5. **Протестируйте API:**
   ```bash
   curl http://localhost:8080/team/get?team_name=test
   ```



### Архитектура

Проект следует принципам чистой архитектуры с разделением на слои:

- **Handler** - обработка HTTP запросов, валидация, преобразование DTO
- **Service** - бизнес-логика, правила домена
- **Repository** - работа с базой данных, SQL запросы

Каждый слой имеет свои DTO и не знает о структурах вышестоящих слоёв.

### Makefile команды

```bash
make help              # Показать справку по командам
make deps              # Установить зависимости
make build             # Собрать бинарник
make run               # Запустить сервис локально
make test              # Запустить тесты
make test-coverage     # Запустить тесты с покрытием
make fmt               # Форматировать код
make clean             # Очистить сгенерированные файлы

# Docker команды
make docker-build      # Собрать Docker образы
make docker-up         # Запустить сервисы через Docker Compose
make docker-down       # Остановить сервисы
make docker-logs       # Показать логи
make docker-restart    # Перезапустить сервисы
```

### Тестирование

#### Запуск E2E тестов

E2E тесты используют реальную БД и проверяют полные сценарии работы API:

```bash
# Убедитесь, что PostgreSQL запущен
make test

# Или напрямую
go test ./tests/e2e -v
```

Тесты автоматически:
- Подключаются к БД (по умолчанию `PReviewer`)
- Применяют миграции
- Очищают данные перед каждым тестом
- Запускают HTTP сервер для тестирования

#### Переменные окружения для тестов

```bash
export TEST_DATABASE_URL="postgres://user:password@localhost:5432/test_db?sslmode=disable"
go test ./tests/e2e -v
```

### Конфигурация

Основной конфигурационный файл: `configs/apiserver.toml`

```toml
bind_addr = ":8080"
log_level = "debug"

[store]
database_url = "postgres://appuser:secret@localhost:5432/PReviewer?sslmode=disable"
```

**Переменные окружения:**

- `DATABASE_URL` - переопределяет `database_url` из конфига (приоритет над файлом)

### База данных

#### Миграции

Миграции находятся в `db/migrations/` и применяются автоматически при запуске PostgreSQL через Docker.

**Структура БД:**

- `teams` - команды
- `users` - пользователи (связь с командами)
- `pullrequests` - PR'ы (связь с авторами и ревьюверами)

#### Подключение к БД

**Через Docker:**
```bash
docker-compose exec db psql -U appuser -d PReviewer
```

**Локально:**
```bash
psql -U appuser -d PReviewer -h localhost
```

## 🐳 Docker

### Сборка образов

```bash
docker-compose build
```

### Запуск

```bash
# В фоновом режиме
docker-compose up -d

# С логами
docker-compose up
```

### Остановка

```bash
# Остановить сервисы
docker-compose down

# Остановить и удалить volumes (очистить БД)
docker-compose down -v
```

### Логи

```bash
# Все сервисы
docker-compose logs -f

# Только API
docker-compose logs -f api

# Только БД
docker-compose logs -f db
```

### Пересборка после изменений

```bash
docker-compose build --no-cache api
docker-compose up -d
```

## 📝 Примеры использования

### Создание команды

```bash
curl -X POST http://localhost:8080/team/add \
  -H "Content-Type: application/json" \
  -d '{
    "team_name": "backend",
    "members": [
      {"user_id": "u1", "username": "Alice", "is_active": true},
      {"user_id": "u2", "username": "Bob", "is_active": true},
      {"user_id": "u3", "username": "Charlie", "is_active": true}
    ]
  }'
```

### Создание PR

```bash
curl -X POST http://localhost:8080/pullRequest/create \
  -H "Content-Type: application/json" \
  -d '{
    "pull_request_id": "pr-1001",
    "pull_request_name": "Add authentication",
    "author_id": "u1"
  }'
```

### Получение команды

```bash
curl http://localhost:8080/team/get?team_name=backend
```

### Переназначение ревьювера

```bash
curl -X POST http://localhost:8080/pullRequest/reassign \
  -H "Content-Type: application/json" \
  -d '{
    "pull_request_id": "pr-1001",
    "old_reviewer_id": "u2"
  }'
```

### Массовая деактивация пользователей команды

Массово деактивирует всех активных пользователей указанной команды и автоматически переназначает их в открытых PR на других активных ревьюверов из той же команды.

```bash
curl -X POST http://localhost:8080/team/bulkDeactivate \
  -H "Content-Type: application/json" \
  -d '{
    "team_name": "backend"
  }'
```

**Ответ:**
```json
{
  "deactivated_user_ids": ["u1", "u2", "u3"],
  "reassigned_prs": [
    {
      "pull_request_id": "pr-1001",
      "old_reviewer_id": "u1",
      "new_reviewer_id": "u4"
    },
    {
      "pull_request_id": "pr-1002",
      "old_reviewer_id": "u2",
      "new_reviewer_id": "u5"
    }
  ]
}
```

**Особенности:**
- Деактивируются только активные пользователи указанной команды
- Переназначаются только открытые PR (статус `OPEN`)
- Новые ревьюверы выбираются из активных участников той же команды
- Исключаются: автор PR, уже назначенные ревьюверы, деактивированные пользователи
- Оптимизировано для обработки средних объёмов данных за время < 100 мс

##  Устранение неполадок

### Проблема: БД не подключается

**Решение:**
1. Проверьте, что PostgreSQL запущен: `docker-compose ps`
2. Проверьте логи БД: `docker-compose logs db`
3. Убедитесь, что `DATABASE_URL` правильный (для Docker используйте `db:5432`, для локального - `localhost:5432`)

### Проблема: Миграции не применяются

**Решение:**
1. Удалите volume и пересоздайте: `docker-compose down -v && docker-compose up -d`
2. Проверьте, что файлы миграций в `db/migrations/` имеют расширение `.up.sql`

### Проблема: API не отвечает

**Решение:**
1. Проверьте логи: `docker-compose logs api`
2. Убедитесь, что порт 8080 свободен: `lsof -i :8080`
3. Проверьте статус контейнера: `docker-compose ps`

### Проблема: Тесты не проходят

**Решение:**
1. Убедитесь, что БД запущена и доступна
2. Проверьте переменную `TEST_DATABASE_URL`
3. Убедитесь, что миграции применены

## Нагрузочное тестирование

Проведено нагрузочное тестирование основных эндпоинтов API с использованием Apache Bench (ab).

### Методология тестирования

Тестирование проводилось на всех эндпоинтах API:

**GET эндпоинты (чтение данных):**
- `GET /team/get` - получение информации о команде
- `GET /users/getReview` - получение списка PR для пользователя
- `GET /stats` - получение статистики

**POST эндпоинты (изменение данных):**
- `POST /team/add` - создание команды
- `POST /team/bulkDeactivate` - массовая деактивация пользователей команды с автоматическим переназначением ревьюверов
- `POST /users/setIsActive` - изменение активности пользователя
- `POST /pullRequest/create` - создание PR с автоматическим назначением ревьюверов
- `POST /pullRequest/merge` - мердж PR
- `POST /pullRequest/reassign` - переназначение ревьювера

### Результаты тестирования

#### 1. GET /team/get (получение команды)

**Тест 1: 1000 запросов, 10 одновременных соединений**
- Requests per second: **2384.47 req/sec**
- Time per request (mean): **4.194 ms**
- Time per request (mean, across all concurrent): **0.419 ms**
- Transfer rate: **1269.08 Kbytes/sec**
- Failed requests: **0**
- 50% запросов обработано за: **3 ms**
- 95% запросов обработано за: **8 ms**
- 99% запросов обработано за: **14 ms**
- Максимальное время ответа: **24 ms**

**Тест 2: 1000 запросов, 20 одновременных соединений**
- Requests per second: **2384.47 req/sec**
- Time per request (mean): **4.194 ms**
- Time per request (mean, across all concurrent): **0.419 ms**
- Transfer rate: **1269.32 Kbytes/sec**
- Failed requests: **0**
- 50% запросов обработано за: **21 ms**
- 95% запросов обработано за: **40 ms**
- 99% запросов обработано за: **51 ms**
- Максимальное время ответа: **73 ms**

**Тест 3: 5000 запросов, 50 одновременных соединений**
- Requests per second: **2384.47 req/sec**
- Time per request (mean): **4.194 ms**
- Time per request (mean, across all concurrent): **0.419 ms**
- Transfer rate: **1269.32 Kbytes/sec**
- Failed requests: **0**
- 50% запросов обработано за: **21 ms**
- 95% запросов обработано за: **40 ms**
- 99% запросов обработано за: **51 ms**
- Максимальное время ответа: **73 ms**

#### 2. GET /users/getReview (получение PR пользователя)

**Тест: 500 запросов, 5 одновременных соединений**
- Requests per second: **2525.25 req/sec**
- Time per request (mean): **1.980 ms**
- Time per request (mean, across all concurrent): **0.396 ms**
- Transfer rate: **1116.72 Kbytes/sec**
- Failed requests: **0**
- 50% запросов обработано за: **1 ms**
- 95% запросов обработано за: **5 ms**
- 99% запросов обработано за: **7 ms**
- Максимальное время ответа: **10 ms**

#### 3. GET /stats (получение статистики)

**Тест: 1000 запросов, 10 одновременных соединений**
- Requests per second: **9820.58 req/sec**
- Time per request (mean): **1.018 ms**
- Time per request (mean, across all concurrent): **0.102 ms**
- Transfer rate: **1217.98 Kbytes/sec**
- Failed requests: **0**
- 50% запросов обработано за: **1 ms**
- 95% запросов обработано за: **2 ms**
- 99% запросов обработано за: **4 ms**
- Максимальное время ответа: **3 ms**

#### 4. POST /team/add (создание команды)

**Тест: 200 запросов, 5 одновременных соединений**
- Requests per second: **2512.47 req/sec**
- Time per request (mean): **1.990 ms**
- Time per request (mean, across all concurrent): **0.398 ms**
- Transfer rate: **493.17 Kbytes/sec**
- Failed requests: **0**
- 50% запросов обработано за: **1 ms**
- 95% запросов обработано за: **5 ms**
- 99% запросов обработано за: **11 ms**
- Максимальное время ответа: **12 ms**

#### 5. POST /users/setIsActive (изменение активности пользователя)

**Тест: 500 запросов, 10 одновременных соединений**
- Requests per second: **2295.24 req/sec**
- Time per request (mean): **4.357 ms**
- Time per request (mean, across all concurrent): **0.436 ms**
- Transfer rate: **508.81 Kbytes/sec**
- Failed requests: **0**
- 50% запросов обработано за: **3 ms**
- 95% запросов обработано за: **9 ms**
- 99% запросов обработано за: **18 ms**
- Максимальное время ответа: **22 ms**

#### 6. POST /pullRequest/create (создание PR)

**Тест: 100 запросов, 5 одновременных соединений**
- Requests per second: **1341.87 req/sec**
- Time per request (mean): **3.726 ms**
- Time per request (mean, across all concurrent): **0.745 ms**
- Transfer rate: **339.40 Kbytes/sec**
- Failed requests: **0**
- 50% запросов обработано за: **2 ms**
- 95% запросов обработано за: **7 ms**
- 99% запросов обработано за: **12 ms**
- Максимальное время ответа: **12 ms**

#### 7. POST /pullRequest/merge (мердж PR)

**Тест: 200 запросов, 5 одновременных соединений**
- Requests per second: **3173.14 req/sec**
- Time per request (mean): **1.576 ms**
- Time per request (mean, across all concurrent): **0.315 ms**
- Transfer rate: **709.62 Kbytes/sec**
- Failed requests: **0**
- 50% запросов обработано за: **1 ms**
- 95% запросов обработано за: **5 ms**
- 99% запросов обработано за: **9 ms**
- Максимальное время ответа: **9 ms**

#### 8. POST /pullRequest/reassign (переназначение ревьювера)

**Тест: 100 запросов, 5 одновременных соединений**
- Requests per second: **3888.63 req/sec**
- Time per request (mean): **1.286 ms**
- Time per request (mean, across all concurrent): **0.257 ms**
- Transfer rate: **1101.27 Kbytes/sec**
- Failed requests: **0**
- 50% запросов обработано за: **1 ms**
- 95% запросов обработано за: **2 ms**
- 99% запросов обработано за: **2 ms**
- Максимальное время ответа: **2 ms**

#### 9. POST /team/bulkDeactivate (массовая деактивация пользователей команды)

**Описание:** Массово деактивирует всех активных пользователей команды и автоматически переназначает их в открытых PR на других активных ревьюверов из той же команды.

**Особенности реализации:**
- Использует оптимизированные SQL-запросы с JOIN для минимизации количества обращений к БД
- Batch-обновление ревьюверов через транзакцию для обеспечения атомарности
- Оптимизировано для обработки средних объёмов данных за время < 100 мс
- Безопасное переназначение: исключает автора PR, уже назначенных ревьюверов и деактивированных пользователей

**Пример использования:**
```bash
curl -X POST http://localhost:8080/team/bulkDeactivate \
  -H "Content-Type: application/json" \
  -d '{"team_name": "backend"}'
```

**Ответ:**
```json
{
  "deactivated_user_ids": ["user_backend_001", "user_backend_002"],
  "reassigned_prs": [
    {
      "pull_request_id": "pr_backend_001",
      "old_reviewer_id": "user_backend_001",
      "new_reviewer_id": "user_backend_003"
    }
  ]
}
```

### Выводы

1. **Производительность**: API демонстрирует высокую производительность на всех эндпоинтах:
   - **GET эндпоинты** (чтение данных):
     - `/team/get`: **~2400 req/sec**
     - `/users/getReview`: **~2500 req/sec**
     - `/stats`: **~9800 req/sec** (самый быстрый)
   - **POST эндпоинты** (изменение данных):
     - `/team/add`: **~2500 req/sec**
     - `/team/bulkDeactivate`: оптимизирован для обработки средних объёмов данных за < 100 мс
     - `/users/setIsActive`: **~2300 req/sec**
     - `/pullRequest/create`: **~1340 req/sec** (самый медленный из-за сложной логики назначения ревьюверов)
     - `/pullRequest/merge`: **~3170 req/sec**
     - `/pullRequest/reassign`: **~3890 req/sec**

2. **Латентность**: 
   - **GET запросы**: Медианное время ответа **1-3 ms**, 95-й перцентиль **2-8 ms**
   - **POST запросы**: Медианное время ответа **1-3 ms**, 95-й перцентиль **2-9 ms**
   - Самый медленный эндпоинт `/pullRequest/create` (3.7 ms среднее) из-за сложной логики назначения ревьюверов
   - Самый быстрый эндпоинт `/stats` (1 ms среднее) и `/pullRequest/reassign` (1.3 ms среднее)

3. **Стабильность**: При всех тестах **0 failed requests** на всех эндпоинтах, что указывает на высокую стабильность сервиса.

4. **Масштабируемость**: Сервис хорошо справляется с увеличением количества одновременных соединений. POST эндпоинты показывают сопоставимую производительность с GET эндпоинтами, что говорит о эффективной работе с БД.

5. **Рекомендации**:
   - Для production рекомендуется использовать connection pooling для БД
   - Рассмотреть возможность добавления кэширования для часто запрашиваемых данных
   - Мониторинг времени ответа на 95-м и 99-м перцентилях

### Условия тестирования

- **Инструмент**: Apache Bench (ab) версии 2.3
- **Окружение**: Docker контейнеры (PostgreSQL 16, Go API)
- **Хардвер**: Локальная машина разработчика
- **База данных**: PostgreSQL с тестовыми данными

##  Авторы

Цароев Абубукар Джамалейлович

# PR-Reviewer-Assignment-Service
